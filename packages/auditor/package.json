{
  "name": "@claws-shield/auditor",
  "version": "1.0.0",
  "type": "module",
  "description": "Flagship AI agent audit engine with 7 analyzers and privacy scoring",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@claws-shield/core": "workspace:*",
    "@claws-shield/intel": "workspace:*",
    "@claws-shield/scanner": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.5"
  }
}
